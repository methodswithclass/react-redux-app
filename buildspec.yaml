version: 0.1

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - cd /build; zip -r ../package.zip *
artifacts:
  files:
    - package.zip